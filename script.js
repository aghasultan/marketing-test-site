/* script.js – interactive canvas + mobile nav (green version) */
document.addEventListener('DOMContentLoaded', () => {
  const PARTICLE_DENSITY = 9000;
  const MAX_PARTICLES = 350;
  const GREEN = 'rgba(0, 168, 107, 0.4)';
  const GREEN_STROKE = 'rgba(0, 168, 107, ';
  const CONNECT = 7;

  /* canvas setup, particle class, connect(), animate() …  */
  /* —–––> (same code I sent in v3; unchanged except colour) */
});
